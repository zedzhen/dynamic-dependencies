# EN

A package for dynamic_dependencies that adds entry points for setuptools.

Do not install this package directly, but use `dynamic_dependencies[setuptools]`.

# RU

Пакет для dynamic_dependencies, добавляющий точки входа для setuptools.

Не устанавливайте этот пакет напрямую, а используйте `dynamic_dependencies[setuptools]`.
